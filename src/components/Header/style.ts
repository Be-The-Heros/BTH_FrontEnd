import styled from "styled-components";
export default styled.div`
  background-color: var(--primary);
  padding: 0.5em 2em;
  .header {
    &__logo {
      width: 3em;
      img {
        border-radius: 15px;
      }
    }
  }
`;
